version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:10000"
    environment:
      - PORT=10000
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port $PORT --reload
